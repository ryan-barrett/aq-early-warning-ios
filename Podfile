# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'aq-early-warning-ios' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for aq-early-warning-ios
  pod 'Firebase/Core'
  pod 'Firebase/Messaging'

  target 'aq-early-warning-iosTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'aq-early-warning-iosUITests' do
    # Pods for testing
  end

end
